[package]
name = "scalar-sdk"
version = "0.1.0"
edition = "2021"
description = "Scalar Network client SDK — boundary crate antara protocol layer dan client-utility layer. Spec §21.2 v9.0."

# DEPENDENCY YANG DIIZINKAN — spec §21.1, §21.2
# scalar-sdk BOLEH depend ke: scalar-crypto, scalar-fees
# scalar-sdk TIDAK BOLEH depend ke: scalar-emission, scalar-stark,
#   scalar-nullifier, scalar-network

[dependencies]
scalar-crypto  = { path = "../scalar-crypto" }
scalar-fees    = { path = "../scalar-fees" }
blake3         = "1.8.4"
thiserror      = "1"
