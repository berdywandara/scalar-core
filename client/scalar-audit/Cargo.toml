[package]
name = "scalar-audit"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Scalar Network audit crate — read-only audit, verify proof, inspect state. Spec §16.4 v11.1-FINAL."

# PRINSIP ISOLASI (spec §16.4):
#   - scalar-audit HANYA menggunakan API publik dari scalar-crypto dan scalar-stark
#   - TIDAK boleh import scalar-nullifier internal state secara langsung
#   - TIDAK ada akses ke kunci privat
#   - Hanya operasi read-only dan ZK verification

[dependencies]
scalar-crypto  = { path = "../../core/scalar-crypto" }
scalar-stark   = { path = "../../core/scalar-stark" }
scalar-emission = { path = "../../core/scalar-emission" }
blake3         = "1.8.4"
thiserror      = "1"
